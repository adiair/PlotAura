<% layout("/layout/boilerplate") -%>

<div class="container-fluid vh-100 d-flex align-items-center justify-content-center">
  <div class="row w-100 shadow-lg rounded-4 overflow-hidden login-wrapper animate-container">

    <!-- Left Side (Signup Form) -->
    <div class="col-md-6 d-flex align-items-center justify-content-center bg-light p-4 animate-left">
      <div class="w-100" style="max-width: 420px;">
<<<<<<< HEAD
        <h2 class="text-center mb-4 fw-bold text-navy fade-in">Sign Up on SkyFlow</h2>
=======
        <h2 class="text-center mb-4 fw-bold text-navy fade-in">Sign Up on Plotora</h2>
>>>>>>> 44d911548869a24f5bd8274637c8522d32a1e2de

        <form action="/signup" method="POST" class="needs-validation" novalidate>
          <!-- Username -->
          <div class="mb-4 fade-in" style="animation-delay: 0.2s;">
            <label for="username" class="form-label fw-semibold">Username</label>
            <input type="text" name="username" id="username" placeholder="Enter username" class="form-control input-custom" required>
          </div>

          <!-- Email -->
          <div class="mb-4 fade-in" style="animation-delay: 0.4s;">
            <label for="email" class="form-label fw-semibold">Email</label>
            <input type="email" name="email" id="email" placeholder="Email address" class="form-control input-custom" required>
          </div>

          <!-- Password -->
          <div class="mb-4 fade-in" style="animation-delay: 0.6s;">
            <label for="password" class="form-label fw-semibold">Password</label>
            <input type="password" name="password" id="password" placeholder="Enter password" class="form-control input-custom" required>
          </div>

          <!-- Submit -->
          <div class="text-center mt-3 fade-in" style="animation-delay: 0.8s;">
            <button class="btn btn-gradient w-100 py-2">Sign Up</button>
            <p class="mt-3">Already have an account? 
              <a href="/login" class="fw-semibold text-decoration-none link-custom">Login</a>
            </p>
          </div>
        </form>
      </div>
    </div>

    <!-- Right Side (Navy Blue Gradient with Social Signup) -->
    <div class="col-md-6 d-flex partition-right flex-column justify-content-center align-items-center text-white px-4 py-5 animate-right">
<<<<<<< HEAD
      <h1 class="fw-bold mb-3 fade-in" style="animation-delay: 0.2s;">Welcome to SkyFlow</h1>
=======
      <h1 class="fw-bold mb-3 fade-in" style="animation-delay: 0.2s;">Welcome to Plotora</h1>
>>>>>>> 44d911548869a24f5bd8274637c8522d32a1e2de
      <p class="lead mb-4 text-center fade-in" style="animation-delay: 0.4s;">Discover places, book stays, and explore the world with us.</p>

      <!-- Social Signup Buttons -->
      <div class="social-signup w-100 fade-in" style="max-width: 320px; animation-delay: 0.6s;">
        <p class="text-center text-light mb-3">Or sign up with</p>
        
        <div class="d-flex gap-3 justify-content-center flex-wrap">
          <!-- Google -->
          <a href="/auth/google" class="btn btn-social btn-google">
            <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/google/google-original.svg" 
                 width="20" height="20" class="me-2">
            Google
          </a>

          <!-- Facebook -->
          <a href="/auth/facebook" class="btn btn-social btn-facebook">
            <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/facebook/facebook-original.svg" 
                 width="20" height="20" class="me-2">
            Facebook
          </a>
        </div>
      </div>
    </div>

  </div>
</div>


<style>
/* Animation Base */
.animate-container {
  animation: fadeInUp 1s ease forwards;
}
.animate-left {
  animation: slideInLeft 1s ease forwards;
}
.animate-right {
  animation: slideInRight 1s ease forwards;
}
.fade-in {
  opacity: 0;
  animation: fadeIn 1s ease forwards;
}
.fade-in[style*="animation-delay"] {
  animation-fill-mode: forwards;
}

/* Keyframes */
@keyframes fadeIn {
  0% { opacity: 0; transform: translateY(15px); }
  100% { opacity: 1; transform: translateY(0); }
}
@keyframes slideInLeft {
  0% { transform: translateX(-50px); opacity: 0; }
  100% { transform: translateX(0); opacity: 1; }
}
@keyframes slideInRight {
  0% { transform: translateX(50px); opacity: 0; }
  100% { transform: translateX(0); opacity: 1; }
}
@keyframes fadeInUp {
  0% { transform: translateY(30px); opacity: 0; }
  100% { transform: translateY(0); opacity: 1; }
}

/* Inputs */
.input-custom {
  border: 1px solid #ccc;
  border-radius: 8px;
  padding: 10px;
  transition: all 0.3s ease;
}
.input-custom:focus {
  border-color: #001f3f;
  box-shadow: 0 0 8px rgba(0,31,63,0.25);
}

/* Button */
.btn-gradient {
  background: linear-gradient(135deg, #000000, #000000);
  border: none;
  color: #fff;
  border-radius: 10px;
  font-weight: 500;
  transition: all 0.3s ease;
}
.btn-gradient:hover {
  background: linear-gradient(135deg, #000000, #000000);
  transform: translateY(-2px);
  box-shadow: 0 6px 12px rgba(0,0,0,0.2);
}

/* Right Partition */
.partition-right {
  background: linear-gradient(135deg, #000000, #000000);
  color: #fff;
  text-align: center;
  animation-delay: 0.3s;
}

/* Social Buttons */
.btn-social {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 10px 20px;
  border-radius: 10px;
  font-weight: 500;
  text-decoration: none;
  transition: all 0.3s ease;
}
.btn-google {
  background: #fff;
  color: #444;
  border: 1px solid #ddd;
}
.btn-google:hover {
  background: #f8f9fa;
  transform: translateY(-2px);
  color: black;
}
.btn-facebook {
  background: #1877f2;
  color: #fff;
}
.btn-facebook:hover {
  background: #0d5dc6;
  transform: translateY(-2px);
}

/* Responsive Fix */
@media (max-width: 768px) {
  .login-wrapper {
    flex-direction: column;
  }
  .partition-right {
    order: 2;
    padding: 40px 20px;
  }
}
</style>
